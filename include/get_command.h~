#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

#include <unistd.h>
#include <termios.h>

char last[100];
char **history=NULL;
int h_size=0;

void add_to_hist(char *s);
int getch();
void get_command(char *s);